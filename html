<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Space Options • Cedar Rapids</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap');
    body { font-family: 'Inter', sans-serif; }
    .space-gradient { background: linear-gradient(135deg, #0f172a 0%, #1e2937 100%); }
  </style>
</head>
<body class="bg-slate-950 text-slate-200">
  <div class="flex h-screen">
    <!-- Sidebar same as before -->
    <div class="w-64 space-gradient p-6 flex flex-col">
      <div class="flex items-center gap-3 mb-10">
        <i class="fa-solid fa-cube text-cyan-400 text-3xl"></i>
        <h1 class="text-2xl font-bold tracking-tight">Space Options</h1>
      </div>
      <p class="text-cyan-300 text-sm mb-8">Cedar Rapids, IA • Closet Shelving</p>
      
      <nav class="space-y-1 flex-1">
        <button onclick="showView('dashboard')" class="w-full text-left flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/10 active-view">Dashboard</button>
        <button onclick="showView('inventory')" class="w-full text-left flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/10">Inventory</button>
        <button onclick="showView('restock')" class="w-full text-left flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/10">Restock / Add Item</button>
        <button onclick="showView('log')" class="w-full text-left flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/10">Activity Log</button>
      </nav>
      <div class="text-xs text-slate-400 mt-auto">Live • Connected to Google Sheet</div>
    </div>

    <!-- Main Content -->
    <div class="flex-1 overflow-auto">
      <header class="bg-white/5 border-b border-white/10 px-8 py-5 flex justify-between items-center">
        <h2 id="pageTitle" class="text-3xl font-semibold">Dashboard</h2>
        <div class="flex items-center gap-4">
          <button onclick="refreshAll()" class="px-5 py-2 bg-cyan-500 hover:bg-cyan-600 text-white rounded-2xl flex items-center gap-2 text-sm font-medium">
            <i class="fa-solid fa-rotate"></i> Refresh Live Data
          </button>
          <div class="text-sm text-slate-400">Space Options • Cedar Rapids, IA</div>
        </div>
      </header>

      <div class="p-8" id="mainContent"></div>
    </div>
  </div>

  <script>
    const WEB_APP_URL = 'https://script.google.com/macros/s/YOUR_NEW_WEB_APP_URL/exec'; // ← REPLACE WITH THE NEW DEPLOY URL

    const SECRET_KEY = 'spaceoptions2026';

    let inventoryData = [];

    async function fetchData(action) {
      try {
        const res = await fetch(`${WEB_APP_URL}?action=${action}`);
        if (!res.ok) throw new Error('Network error');
        return await res.json();
      } catch (e) {
        alert('Could not connect to Google Sheet. Make sure the Web App is deployed with "Anyone" access.');
        return [];
      }
    }

    async function postData(payload) {
      payload.secret = SECRET_KEY;
      const res = await fetch(WEB_APP_URL, {
        method: 'POST',
        body: JSON.stringify(payload),
        headers: { 'Content-Type': 'application/json' }
      });
      return await res.json();
    }

    // The rest of the script (loadInventory, renderView, restock functions, etc.) is the same as the previous version I gave you.

    // For brevity, copy the entire <script> section from my previous message (the long one with showView, renderView, performRestock, etc.)
    // and paste it here, replacing only the WEB_APP_URL line above.

    // Initial load
    window.onload = () => {
      showView('dashboard');
    };
  </script>
</body>
</html>
